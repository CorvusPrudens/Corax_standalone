
int global = 100;

// Pointer to const integer
const int* ptr1;
// const pointer to integer
int* const ptr2;

int* ptr = (int*) 5;

void uart(int val)
{
  // how to interface with hardware? inlining?
}

void orphan()
{
  unsigned int a;
  int b = 20;
  a = 1 + 2 * b;

  a = 1 + 2 * *ptr;
}

void loop()
{

  int start = global;

  start = add(start, 1) * 2;

  uart(start);

}