
#define MACRO 100

int variable;
int var2 = 0;

void function();
void other();

void function() {
    begin:
    variable = var2 + 2 + MACRO;
    variable += (var2 + 1) * 2;

    for (int i = 0; i < variable; i++)
        if (i > 2)
        {
            other();
        }
}

void loop() {
    #ifdef MACRO
    function();
    #endif
    var2 += 1;
}