
short global;

short add(short x, short y)
{
  return x + y;
}

short expr(short x, short y)
{
  short temp = add(x, y);
  return temp++; // note -- temp should be returned before the increment (which will be skipped)
}

void loop() 
{
  global = expr(2, global + 2) + expr(1, 2);
}